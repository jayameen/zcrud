spring:
  application:
    name: zcrud
  servlet:
    multipart:
      max-file-size: 10000000
      max-request-size: 10000000

server:
  port: 8081
  servlet:
    context-path: /${spring.application.name}

---
spring:
  config:
    activate:
      on-profile: default
: